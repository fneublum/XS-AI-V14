# @azure/msal-react@2.0.12 only declares peer react@^16|^17|^18 but works
# fine on React 19 (it's a runtime compatibility check we satisfy via the
# version-tolerant MSAL browser package). Until upstream releases React 19
# peer support, use legacy resolution so npm ci doesn't block the build.
legacy-peer-deps=true
